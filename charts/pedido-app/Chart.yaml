apiVersion: v2
name: pedido-app
description: Chart para sistema de pedidos
type: application
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: postgresql
    version: "11.9.13"          # ← versión antigua que usa Docker Hub
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
