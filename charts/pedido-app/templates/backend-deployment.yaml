apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pedido-app.fullname" . }}-backend
spec:
  replicas: {{ .Values.backend.replicaCount | default .Values.replicaCount | default 2 }}
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}"
        imagePullPolicy: {{ .Values.backend.image.pullPolicy | default "IfNotPresent" }}
        ports:
        - containerPort: {{ .Values.backend.port | default 80 }}
        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://$(DB_HOST):5432/$(DB_NAME)
        - name: DB_HOST
          value: {{ .Values.postgresql.auth.username | default "pedido" | quote }}
        - name: DB_NAME
          value: {{ .Values.postgresql.auth.database | default "pedidos" | quote }}
        - name: DB_USER
          value: {{ .Values.postgresql.auth.username | default "pedido" | quote }}
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "pedido-app.fullname" . }}-db-secret
              key: DB_PASSWORD
        resources:
          {{- toYaml .Values.backend.resources | nindent 10 }}
        # FIX 100%: Nginx no actuator
        livenessProbe:
          httpGet:
            path: /  # ← CAMBIADO
            port: {{ .Values.backend.port }}
          initialDelaySeconds: 30  # Nginx rápido
        readinessProbe:
          httpGet:
            path: /  # ← CAMBIADO
            port: {{ .Values.backend.port }}
          initialDelaySeconds: 10
